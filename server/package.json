{
  "name": "departures-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/worker.js",
    "deploy": "wrangler deploy",
    "check": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.51.0"
  }
}

